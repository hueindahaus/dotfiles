return {
  cmd = { "pylint" },
  filetypes = { "python" },
  settings = {
    -- python = {
    --   analysis = {
    --     -- Ignore all files for analysis to exclusively use Ruff for linting/formatting/organizing imports
    --     ignore = { '*' },
    --   },
    -- },
  }
}
